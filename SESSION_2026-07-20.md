# SESSION_2026-07-20 — Post-audit fixes, homepage scroll-story hero, docs pass

*Experimental v2 (`rovaneD/agent7even-v2`). Work since CONTEXTV28 (July 13) through July 20.*

---

## Context

Prior handoff: **CONTEXTV28** (canonical profile resolution, dead routes, pre-push guard). This period covered marketing homepage iteration, a full read-only product audit, four fix phases, Maya/scheduling hardening, and swapping the live homepage to scroll-story hero variant B.

---

## Commits (remote, newest first)

| Commit | Summary |
|--------|---------|
| `a3e1eca` | Hero spacing — 80px gap copy → mockup; mobile stage margin |
| `dda3550` | Eyebrow + Maya subline line breaks (no widows) |
| `f75258b` | Mobile auto-play story (no scroll scrub); orb/type overlap fixes |
| `74573d5` | **Homepage swap** — `/` → `HomepageSiteBrandStoryB`; old hero at `/lab5` |
| `dc07d97` | Cookie banner + GA only on marketing routes |
| `aefc17a` | Maya context race fix (global snapshot + render-time clear) |
| `c2d3a55` | Audit Phase 4 — polish (schedules UI, notifications, OG, a11y) |
| `eb6f509` | Audit Phase 3 — server-side tier/access enforcement |
| `cddfa60` | Audit Phase 2 — product promise alignment |
| `2b6a0cd` | Audit Phase 1 — security + billing lifecycle |
| `c2fa91c` | Agent schedules seeding + Maya context improvements |
| `40972b8` | MayaOrb in marketing hero mockup |
| `41accea` | Admin notify on new signup |
| `c303dbd` | lab5 pricing analytics tracking |
| `f9a79b1` | Desktop metaballs + hero spacing after mobile LCP |
| `e609bd5` / `4026459` | Mobile homepage LCP (SSR mockup, deferred JS) |
| `10880ee` | Cookie consent + consent-gated GA4 + Vercel Analytics |
| `aab4f02` | Teams journey section |
| `68c876d` … `c244696` | lab5 hero rebuild + mobile layout passes |

---

## Major workstreams

### Product audit (read-only, then phased fixes)

Six-domain audit synthesized into interactive canvas report. Phases 1–4 shipped sequentially (see `CONTEXTV29.md` §1). Highlights:

- Digest generation no longer unauthenticated.
- Churned users' schedules deactivated; cron skips ineligible workspaces.
- Duplicate Stripe subscriptions prevented on returning checkout.
- UI claims (trial Brand Kit lock, toolkit tiers, service limits, seats) enforced server-side.
- Schedule management UI + failed-run + trial-ending notifications.

### Agents + scheduling

- `ensureDefaultAgentSchedules` on Foundation complete + Agents page load.
- Performance Digest prompt tightened against fabrication without analytics.
- Agent Command Center Foundation banner + schedule panel.

### Maya context

- Content Posting dispatches step-aware context.
- Panel re-greets on navigation when user hasn't engaged.
- Fixed effect-ordering bug: `__MAYA_CANVAS_CONTEXT__` + DashboardShell render-phase reset.

### Marketing homepage

1. **lab5 iteration** — metaballs, MayaOrb, mobile LCP, pricing tracking, Teams section, cookie banner.
2. **Variant B concept** — scroll-driven story from design-concept; A/B screenshot layout.
3. **Production swap (`74573d5`)** — `app/page.tsx` imports `HomepageSiteBrandStoryB`.
4. **Mobile fix (`f75258b`)** — auto-play instead of 750vh sticky scroll on ≤860px.
5. **Polish (`dda3550`, `a3e1eca`)** — line breaks, hero-to-stage spacing.

**File SSOT:** `app/design-concept/homepage-site-brand-b/HomepageSiteBrandStoryB.tsx` + `homepage-site-brand-b.css`.

### Analytics / chrome

- `DeferredChrome.tsx` route-aware — no cookie banner or GA on app routes.
- OG image public in `proxy.ts` (on main); local uncommitted addition for `/design-concept(.*)`.

---

## SQL / external checks

| Item | Action |
|------|--------|
| `45_agent_run_failed_notification_type.sql` | Run in Supabase if not applied |
| Stripe webhook | Include `customer.subscription.trial_will_end` |
| Vercel env | `OPENROUTER_VIDEO_WEBHOOK_SECRET`, credit price IDs |

---

## Docs updated this session

- `CONTEXTV29.md` — new handoff (this work)
- `SESSION_2026-07-20.md` — this log
- `AGENTS.md` — index, last-reviewed date, new implementation notes

---

## Known follow-ups

- Mobile hero stage scaling and animation tuning.
- Untracked design-concept GSAP experiments break local full-repo `tsc`/`build` — not on main.
- User noted homepage "needs further work" after initial mobile ship — spacing/copy iterations ongoing.
