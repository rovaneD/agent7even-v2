# SESSION_2026-07-24 — Onboarding v2, trial billing, agents UX, docs pass

*Experimental v2 (`rovaneD/agent7even-v2`). Work since CONTEXTV29 (July 20) through July 24.*

---

## Context

Prior handoff: **CONTEXTV29** (post-audit phases 1–4, scroll-story homepage, Maya context fixes). This period shipped website-first Foundation onboarding, tier-neutral 7-day trial billing, admin account delete, Maya hub actuation, marketing/auth copy alignment, agent schedule overdue fixes, auto-output clarity, admin subscription gate bypass, and Agents Command Center scroll fix.

---

## Commits (remote, newest first)

| Commit | Summary |
|--------|---------|
| `704c316` | Remove nested scroll traps on Agents Command Center panels |
| `4ee6a55` | Admin/owner bypass trial subscription gate |
| `182b07a` | Fix schedule overdue state; clarify auto vs approval outputs |
| `e262765` | Restore auth pink headline; center homepage Maya subline |
| `ecb48b4` | Refresh auth + homepage copy for current product |
| `5e214d1` | Website-first onboarding, trial billing, admin delete, Maya hub |
| `879cbbb` | Drop fake counts from homepage feature checklists |
| `890fb3a` | Add claude-watch project skill |
| `cefa02b` | V29 docs pass; public `/design-concept` routes |
| `a3da327` | Homepage feature-block copy variety; FAQ de-duplication |

---

## Major workstreams

### Website-first Foundation onboarding (`5e214d1`)

1. URL → Exa read + synthesis → normalized answers (`onboardFromWebsite.ts`).
2. Confirm screen with checklist — “Looks good” CTA, no 6s loading cap.
3. `complete-onboarding` API — score, generate docs, mark Foundation complete, seed agent schedules.
4. Replaced old research race in `FoundationFlow.tsx`.

**File SSOT:** `lib/foundation/onboardFromWebsite.ts`, `synthesizeOnboardingAnswers.ts`, `runFoundationGeneration.ts`, `FoundationOnboardConfirm.tsx`.

### Trial billing v2 (`5e214d1`, `4ee6a55`)

- `lib/billing/trialPolicy.ts` — 7-day tier-neutral trial; charge day 8; 5 toolkit runs during trial.
- `/start-trial` gate for profiles without Stripe subscription after sign-up.
- Checkout sync / recovery via `activateCheckoutSession.ts`.
- `profileBypassesSubscriptionGate()` — admin, owner, billing_exempt skip gate.
- Auth + homepage copy import trial constants (no hardcoded “3-day Starter-only”).

### Admin tooling (`5e214d1`)

- Permanent client delete with Stripe/Zernio/Clerk teardown + confirmation modals.
- Admin notify on new signup (dev alert path).

### Maya Foundation Hub (`5e214d1`)

- Hub form actuation patches from Maya chat.
- Streaming JSON strip in `formActuation.ts`.

### Marketing + auth copy (`ecb48b4`, `e262765`)

- Foundation / agents / approval queue bullets on sign-up.
- “You approve before anything goes live” (ongoing, not once).
- Pink “AI-powered marketing for small business” headline restored.
- Homepage Maya subline centered with orb.

### Agents Command Center (`182b07a`, `704c316`)

- Schedule `next_run_at` always advanced on cron (including skip/failure).
- `reconcileStaleAgentSchedules` on Agents page load.
- Auto-agent outputs labeled Saved; approval queue copy honest for autonomous agents.
- Removed nested scroll regions in Live activity + Scorecard.

---

## SQL / external checks

| Item | Action |
|------|--------|
| `45_agent_run_failed_notification_type.sql` | Run in Supabase if not applied |
| Stripe webhook | Include `customer.subscription.trial_will_end` |
| Stripe checkout | All tiers use 7-day trial in v2 checkout flow |
| Vercel env | `OPENROUTER_VIDEO_WEBHOOK_SECRET`, credit price IDs |

---

## Docs updated this session

- `CONTEXTV30.md` — new handoff (this work)
- `SESSION_2026-07-24.md` — this log
- `AGENTS.md` — index, trial policy, last-reviewed date, new implementation notes
- `README.md` — branch, read-first pointers

---

## Known follow-ups

- Mobile hero stage scaling (from V29).
- Port onboarding v2 + trial v2 to production repo when ready.
- Untracked local files (`.cursor/settings.json`, `Maya_Product.md`, vendor xlsx) — not committed.
