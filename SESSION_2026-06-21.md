# SESSION_2026-06-21 — Creative assets + hardened image generation

*Experimental v2 (`rovaneD/agent7even-v2`). Initial ship `f300349`; follow-ons through `a32263f` on `main`.*

*Status ledger updated: June 22, 2026*

---

## Shipped

### Creative Assets library

- `/dashboard/assets` — sidebar nav entry
- Save any generated option from picker
- Folders (create, rename, delete, move)
- Preview modal, delete asset, download
- **Use for post** via `?useAsset=<id>` on Agents

### Generation workflow

- Session persistence (Agents ↔ Assets) + discard + refresh previews
- Edit panel: Fix text only (Recraft) / Change visual (Gemini img2img)
- Save tracks `storagePath` not index (fix after edit)
- Download at picker, Assets, Posts

### Quality / safety (v1.1 + follow-ons)

- Brief validation — Photoreal infographic briefs replaced; hex/token/font specs stripped
- Post-gen QA on all 3 options before display; one auto-regen per failing option
- `designSpecLeakDetection` — bare hex labels (`2D3748 Investment`)
- Provider error sanitization — no raw OpenRouter messages in UI
- **Logo lockup blocking** (`74e5aa7`) — brief + QA guards; fixed QA retry using weak fallback brief
- **Post grounding** (`a32263f`) — briefs + QA tie to Post goal, Offer/CTA, Audience; block invented logos when Brand Kit on / logo off; fail generic filler (“Real talk over coffee”, “Boost your brand”)

### Maya chat (`75bed92`)

- Sidebar chat: first person, fewer questions, no fake “spinning up agents”
- Assets page context wired into Maya

### Capability copy (`50ea600`)

- `lib/posts/imageContextCapabilities.ts` — flag-aware prompts; Maya directs to Agents when generation is on

### SQL (manual Supabase)

- `19_creative_assets.sql`
- `20_creative_assets_extend.sql`
- `21_creative_asset_folders.sql`

### Commits on `main` (this arc)

| Commit | Summary |
|--------|---------|
| `f300349` | Creative assets library + image gen v1.1 |
| `50ea600` | Flag-aware `imageContextCapabilities` |
| `75bed92` | Maya sidebar chat fixes |
| `74e5aa7` | Block logo lockup outputs |
| `a32263f` | Ground posts in form context; block invented logos |

---

## Status ledger (June 22, 2026)

### Immediate decisions (blocking / time-sensitive)

| Item | Status | Notes |
|------|--------|--------|
| **Production flag** `NEXT_PUBLIC_IMAGE_GENERATION` | **Done** | Production env confirmed **`"true"`** (not empty string) — June 22, 2026. Generate UI + APIs active after redeploy. Rollback: set `false` + redeploy. |
| **SQL 19 / 20 / 21** | **Done (session)** | Applied in Supabase per checklist below. Verify: Assets folders create/move without error. |
| **Zernio DPA §13 governing law** | **Blocked externally** | Do not sign until resolved. Real multi-tenant publish gated until lawyer + Zernio close this. |
| **Zernio IG analytics zeros** | **Root cause confirmed** | June 15 reconnect lacked `instagram_business_basic` (Meta consent unchecked). Fix: reconnect + accept all permissions. Zernio shipping "reconnect needed" UI. See `zernio_social_evaluation_backlog.md`. |

### Active build items (code)

| Item | Status | Notes |
|------|--------|--------|
| **Brief composer overhaul** | **In progress — first pass shipped** | Validation, logo lockup, post grounding, Brand Kit no-logo rules, vision QA with post context (`74e5aa7`, `a32263f`). Not a full compose-model rewrite. **Next:** prod smoke with **Post goal + Offer/CTA filled**; iterate on output quality. |
| **`imageContextCapabilities.ts`** | **Done** | `50ea600`. Runtime prompts flag-aware. Static baseline object unchanged for upload validators — intentional. Not a blocker for prod flag. |
| **Stale admin digest query** | **Parked — still broken** | `app/api/digest/generate/route.ts` filters `status = 'approval_required'` (never written). Pending section empty forever. |
| **Admin `field_name` / `field_key`** | **Parked — still broken** | `app/admin/clients/[id]/page.tsx` selects `field_name`; DB column is `field_key`. |

### Gated (waiting on others)

| Item | Status |
|------|--------|
| EnsembleData Stage 1 + 3 | Gated on ToS/legal for derived-insight redistribution |
| Scoped-key Phase 2 Zernio migration | Gated on written isolation guarantee from Zernio |
| Zernio `DELETE /profiles/{id}` ok: false | Not investigated — small diagnostic when bandwidth allows |
| Zernio headless connect UX | **Confirmed by vendor** | Build per-platform pickers in-app; no hosted connect portal on Zernio roadmap |

### Deferred / parked (explicitly not now)

Post media Phases A–C, Website Audit Agent, hero user counter, Foundation Intelligence full loop, video generation, onboarding wizard — **unchanged**; start after generation quality is proven in prod.

### This week (solo founder priority)

1. **Verify SQL** — folders work in prod Assets (~5 min).
2. ~~**Production flag decision**~~ — **Done:** Production value confirmed `"true"`. Ensure latest `main` deploy (`a32263f`) is live after any env change.
3. **Brief composer iteration** — prod smoke → tune; not greenfield “start overhaul.”

---

## Verification

- [x] `npx tsc --noEmit`
- [x] `npm run build`
- [x] Push `main` → `rovaneD/agent7even-v2` (through `a32263f`)
- [x] Run SQL migrations in Supabase (`19`, `20`, `21`)
- [x] Capability prompts flag-aware (`imageContextCapabilities.ts`)
- [ ] Manual smoke: generate → edit → save → folder → use for post
- [x] Production `NEXT_PUBLIC_IMAGE_GENERATION=true` confirmed in Vercel (exact string `"true"`, not empty — June 22, 2026)
- [ ] Prod smoke with **Post goal + Offer/CTA** filled (required for new grounding rules)

---

## Docs updated this session

- `CONTEXTV19.md` (new)
- `MAYA_CONTEXT_V10.md` (new)
- `creative_generation_handoff.md` (v1.1 addendum)
- `post_media_expansion_handoff.md` (generation cross-ref)
- `AGENTS.md` (pointers + SQL list)
- `SESSION_2026-06-20-creative-generation.md` (pointer to v1.1)

---

## Production flag + redeploy checklist

**Project:** Vercel `agent7even-v2` · **GitHub:** `rovaneD/agent7even-v2` · **Prod host:** `www.agent7even.ai`

### Before flipping the flag

- [x] SQL `19`, `20`, `21` applied in Supabase
- [x] Capability prompts flag-aware (`imageContextCapabilities.ts`)
- [x] OpenRouter credits sufficient (no 402 in staging)
- [x] Foundation scores exist for test account (generation floor gate)
- [x] Logo lockup + post grounding shipped (`74e5aa7`, `a32263f`)

### Vercel env (Production only)

**Confirmed June 22, 2026:** Production value is exactly `true` (empty string previously broke the flag silently).

1. [Vercel dashboard](https://vercel.com) → **agent7even-v2** → **Settings** → **Environment Variables**
2. Find `NEXT_PUBLIC_IMAGE_GENERATION`
   - If missing: **Add** → Name `NEXT_PUBLIC_IMAGE_GENERATION`, Value `true`, Environment **Production** only
   - If present: **Edit** → set Production value to `true` (not empty string — must be exactly `true`)
   - Leave Preview/Development as needed for staging
3. Confirm related vars still set: `OPENROUTER_API_KEY`, Supabase keys, Clerk prod keys

Or CLI (Production):

```bash
cd ~/agent7even-v2-clean
git remote -v   # must show rovaneD/agent7even-v2
vercel env update NEXT_PUBLIC_IMAGE_GENERATION production --value true -y
```

### Redeploy (required)

`NEXT_PUBLIC_*` is inlined at **build time**. Changing the env var alone is not enough — trigger a Production deployment:

- **Option A:** Vercel → **Deployments** → latest `main` → **⋯** → **Redeploy** → Production
- **Option B:** push to `main` (CI builds + deploys)

Wait for deploy green on `www.agent7even.ai`.

### Prod smoke (signed-in test account)

Fill **Post goal** and **Offer / CTA** in the single-post form before generating.

| Step | Pass? |
|------|-------|
| Agents → Single post → **Generate with Maya** block visible | |
| Generate 3 options completes (headlines tied to post, not generic filler) | |
| No invented logo when Brand Kit on + **Include logo** off | |
| Pick option → text QA runs | |
| Save to **Assets** → appears in library | |
| Create folder → move asset | |
| **Use for post** from Assets → loads in Agents | |
| Submit for approval → item in Approvals with image | |
| Maya chat: ask “generate a post image” → directs to Agents (does not claim generation is impossible) | |

### Rollback

Set Production `NEXT_PUBLIC_IMAGE_GENERATION=false` → **Redeploy Production** again.

---

*Session started June 21, 2026 · status ledger June 22, 2026*
