# SESSION_2026-07-04 — Email migration, Guardian checkpoint, proposal surface

*Experimental v2 (`rovaneD/agent7even-v2`). Active working session.*

---

## Context

Prior handoff: **CONTEXTV23** (July 3). This session closed July 3 carry-over items, ran the Guardian human checkpoint, and shipped Build Sequence item 3 (proposal surface) + item 4 v0 (layer persistence).

**Deploy workflow:** direct `git push origin main`.

---

## Commits

| Commit | Summary |
|--------|---------|
| `435d037` | Transactional email migration, profile merge scripts/runbook, `35_notifications_type_check.sql` |
| *(pending)* | Foundation proposal surface + layers + docs |

---

## Ops applied (Supabase)

| Migration | Status |
|-----------|--------|
| `34_foundation_site_snapshot.sql` | Applied (prior session) |
| `35_notifications_type_check.sql` | Applied this session — `approval_pending` in-app inserts work |
| `36_foundation_proposal_decisions.sql` | **Run required** for proposal UI |
| `37_foundation_layers.sql` | **Run required** for Approve → layer |

---

## Closed from CONTEXTV23 open items

| Item | Resolution |
|------|------------|
| Duplicate `profiles` merge | **Not needed** — `list-duplicate-profiles.ts` → 0 groups; orphan already deleted. Two `company_name: Agent7even` rows are separate accounts (Melissa vs Rovane). |
| Transactional email migration | **Done** — all legacy `.com` / terracotta routes → `sendTransactionalEmail` + blue `.ai` template |
| `notifications_type_check` | **Done** — SQL applied in Supabase |
| Profile merge in DB | N/A — canonical code path + clean DB |

---

## C — Changelog ↔ proposals sanity review

**Profile:** `bfa73081-3906-4b5b-b24e-d9df3fb07384` (Rovane / ProAgent / `company_name: Agent7even`)

### Changelog (5 rows)

| Date | Signal | Agent | Summary |
|------|--------|-------|---------|
| Jul 4 | approved | campaign_builder | Approved pricing-focused Campaign Builder output |
| Jul 4 | rejected | campaign_builder | Rejected "Stop Feeding the Marketing Mon…" |
| Jul 4 | rejected | campaign_builder | Rejected "Fire Your Agency, Hire Maya" |
| Jul 4 | rejected | campaign_builder | Rejected pricing-focused output |
| Jul 4 | rejected | foundation_generate_brief | Rejected brief probe output |

**Read:** Real decision signal from Campaign Builder testing — agency-bashing and pricing hooks rejected; one pricing output approved then contradicted by later reject (noisy but honest).

### Proposals (5 rows after two Guardian runs)

| Verdict | State | Changelog rows | Theme | Title |
|---------|-------|----------------|-------|-------|
| **surface** | extending | **4** | campaign_builder_pattern:rejected | Vary primary hooks — avoid repeating rejected themes |
| reject_internal | extending | 2 | pricing_angle:rejected | Pricing Communication Guidelines |
| reject_internal | extending | 1 | pricing_angle:rejected | Pricing Messaging — value focus |
| reject_internal | extending | 2 | campaign_builder_pattern:rejected | (earlier duplicate cluster) |
| reject_internal | consistent | 1 | agency_framing_rejected | Agency-bashing hooks underperformed |

**Verdict:** Guardian thresholds behaved correctly. Only the 4-row campaign-builder cluster surfaced. Pricing and agency themes stayed internal (1–2 rows). Proposal content matches changelog — not hallucinated.

**Threshold decision:** **Hold** ≥3/≥4 defaults until more organic (non-test) signal accumulates.

**Sequencing note:** Observer v0.5 + Guardian v0 shipped same commit (`34f17ac`) before explicit checkpoint — acceptable because Guardian is batch-only until this session's UI.

---

## D — Proposal surface shipped

| Piece | Path |
|-------|------|
| Decision columns | `36_foundation_proposal_decisions.sql` |
| Layers table | `37_foundation_layers.sql` |
| Load / decide | `lib/foundation/proposals/*`, `lib/foundation/layersContext.ts` |
| API | `GET /api/foundation/proposals`, `POST /api/foundation/proposals/[id]/decide` |
| UI | `components/foundation/FoundationProposalsPanel.tsx` in Foundation Hub (Intelligence tab) |
| Agent context | Approved layers in `buildAgentContext.ts` + Maya chat |

**UX:** Approve layer · Not now (defer) · Dismiss (reject). Panel hidden when no pending surfaced proposals. Calm copy — not a notification feed.

---

## Verification

```bash
npx tsc --noEmit
npm run build
git remote -v   # rovaneD/agent7even-v2
```

**Manual:**

1. Run `36` + `37` SQL in Supabase
2. Foundation Hub → Intelligence → see pending proposal (campaign hook variation)
3. Approve → confirm row in `foundation_layers`; agents/Maya context includes layer block
4. Trigger approval-pending agent run → in-app notification inserts without CHECK error

---

## Next

1. More organic approve/reject activity → re-run Guardian batch
2. Tune `GUARDIAN_THRESHOLDS` only after second checkpoint with real usage data
3. Reject cooldown for Observer (open question in vision doc)
4. Proposal surface polish: optional defer/resurface cadence, Maya nudge when pending proposals exist
