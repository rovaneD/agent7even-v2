# SESSION_2026-07-04 — Critical bug investigation

*Experimental v2 (`rovaneD/agent7even-v2`). Cron-triggered high-severity bug pass.*

---

## Scope

Reviewed recent July 3/4 changes around duplicate profile resolution, GA OAuth, Foundation site snapshot, and Agents run sub-pages. Opened PR #15 from `cursor/critical-bug-investigation-6516`.

---

## Critical fixes

| Area | Fix |
|------|-----|
| GA OAuth callback | Callback now requires the live Clerk session to match the consumed OAuth state owner before saving tokens. Token save resolves by nonce-bound Clerk ID + Clerk email, never Google OAuth email fallback. |
| Foundation site snapshot fetch | Direct website fetch now validates public HTTP(S) URLs, rejects private/internal DNS/IP targets, follows redirects manually with the same validation, and caps HTML reads before buffering. |
| Foundation snapshot storage | `SiteSnapshotSchema` now caps string and array sizes so PATCH cannot persist huge JSON into agent prompts. |
| Team/workspace snapshot reads | Site snapshot GET/POST/PATCH now resolve the canonical actor profile and read/write the workspace owner row consistently. `buildAgentContext` also resolves the workspace owner profile before loading snapshot/context. |
| Agents run/profile resolution | Agents hub, run page, task create/status/list/output APIs, and constraints API now use dashboard canonical profile resolution plus workspace owner IDs instead of duplicate-sensitive `.single()` profile reads. |
| Duplicate agent runs | Guided run and Content Posting clients now use a synchronous in-flight ref guard to prevent rapid double-submit duplicate task creation. |

---

## Validation

```bash
npm ci
npx tsc --noEmit
npm run build
```

Build passed on Next.js 16.2.6.

---

## Notes for future work

- Keep OAuth callbacks bound to the nonce owner **and** the active Clerk session before writing any profile tokens.
- Do not use user-selected provider email (for example Google OAuth email) as a tenant profile lookup fallback.
- Any server-side website fetch must validate public HTTP(S), DNS/IP targets, and redirect hops, and must cap response reads before buffering.
- For workspace-scoped agent/foundation data, resolve the canonical actor profile first, then use `resolveWorkspaceProfileId`.
