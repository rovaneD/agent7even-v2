# SESSION_2026-06-23 — Content Posting workflow UX

*Experimental v2 (`rovaneD/agent7even-v2`).*

---

## Shipped

### Content Posting 3-step flow

- **Hub** `/dashboard/agents/content-posting` — Image post, Video post, Weekly plan cards
- **Format picker** — platform-grouped rows, sticky live preview, stepper (Workflow → Format → Setup)
- **Setup** — existing generate/compose flow gated on `?format={id}` via `ContentPostingModeGate`

### Platform coverage

- 8 image formats + 6 video formats with pixel dimensions and generation aspect ratios
- Platform brand icons (Simple Icons paths, crisp SVG at 26px)
- Platform-native preview chrome (`PlatformPostPreview`)

### UX polish

- Increased vertical gap between stepper and format grid (`mt-10`)
- Left selector column bottom-aligns with preview card on desktop (`lg:items-end`)
- Weekly setup back link → **Back to workflows** (same as image/video)

### Integration

- Agents Command Center routes to content-posting hub
- `AgentsLegacyRedirects` for old query-based entry
- Asset preview **Use for post** → image flow with default `ig-feed-post` format

---

## Key paths

```
app/dashboard/agents/content-posting/
components/agents/contentPosting/
lib/agents/contentPosting/platformFormats.ts
lib/agents/contentPosting/platformBrandIcons.ts
CONTEXTV20.md
```

---

## Verification

- [x] `npx tsc --noEmit`
- [x] `npm run build`
- [ ] Manual: hub → image format → setup → back to workflows
- [ ] Manual: weekly → back to workflows (not Agents)
- [ ] Manual: format picker icons sharp at 1x and 2x display

---

## Follow-ups

- Setup step (step 3) visual polish to match Claude Design setup screens
- Richer preview chrome per platform (Reels side rail, X header mock, etc.)
- Pass `expectedHeadline` through client QA call (from prior backlog)
