# SESSION_2026-06-23 — Content Posting UX, Creative Direction cache, Maya orb

*Experimental v2 (`rovaneD/agent7even-v2`).*

---

## Shipped (pushed)

### Content Posting 3-step flow (`506c792`)

- **Hub** `/dashboard/agents/content-posting` — Image post, Video post, Weekly plan cards
- **Format picker** — platform-grouped rows, sticky live preview, stepper (Workflow → Format → Setup)
- **Setup** — existing generate/compose flow gated on `?format={id}` via `ContentPostingModeGate`
- Platform brand icons, native preview chrome, weekly back link → **Back to workflows**

### Creative Direction cache + hub synthesis (`c9e0259`)

- Cache on `profiles` — recompute on content-checked Foundation saves via `after()`
- Generation reads `getOrComputeCreativeDirection()` (image + video)
- Your Look hub card: `Look: {aesthetic} · {palette} · {casting}` from cached `visualDirection`
- SQL: `23_creative_direction_cache.sql` — **apply in Supabase before deploy**

---

## Shipped (this commit — Maya orb)

### `MayaOrb` component

- `components/maya/MayaOrb.tsx` + `maya-orb.css`
- Brand equalizer: blue `#3B82F6` + pink accent `#F5349B`, inner ring, 24 radial bars
- States: idle pulse, `active` (faster), `inverted` (light bars on dark sidebar)

### App wiring

- Sidebar Maya button (`DashboardShell.tsx`) — replaces Sparkles
- Chat header, mode picker, message avatars, thinking state (`MayChatPanel.tsx`)

### Marketing mockups (lab5)

- Orb replaces dark **M** circle in Maya chat header (`mockups.js`, `usecase-mockups.js`)
- `useMockupScript.ts` — loads mockup JS via `useEffect` (Next 16 client `<Script>` does not execute)
- `.mk-ava` transparent; orb animation CSS in `app/lab5/styles.css`

---

## Key paths

```
components/maya/MayaOrb.tsx
components/maya/maya-orb.css
app/lab5/useMockupScript.ts
public/lab5/mockups.js
lib/agents/foundationCreativeDirection/
app/dashboard/agents/content-posting/
CONTEXTV20.md
```

---

## Verification

- [x] `npx tsc --noEmit`
- [x] `npm run build`
- [x] Local `/` — 13 mockup widgets, hero dashboard with orb in chat header
- [x] Local `/dashboard` — sidebar + chat orb visible
- [ ] Apply `23_creative_direction_cache.sql` in Supabase
- [ ] Manual: hub → image format → setup → back to workflows

---

## Follow-ups

- Optional: round MayaOrb SVG coords to fix dev hydration mismatch warning
- Content Posting setup step visual polish (Claude Design reference)
- Richer preview chrome per platform (Reels side rail, X header mock, etc.)
- Pass `expectedHeadline` through client QA call (from prior backlog)
