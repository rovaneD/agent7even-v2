# SESSION_2026-06-18 — Homepage, auth/billing, analytics & Zernio connect

*Experimental v2 (`rovaneD/agent7even-v2`). Production app untouched.*

---

## Shipped to `main` (June 18 — full day)

| Commit | Summary |
|--------|---------|
| `a8ee8c6` | GA tab: hostname + session source/medium widgets; revoke stale grant on reconnect; clearer OAuth token errors |
| `ae2d614` | GA OAuth redirect URI from request host (fixes `.ai` redirect_uri_mismatch) |
| `0eba105` | GA: stop false “access pending” when OAuth refresh fails (no SA fallback) |
| `e669826` | Analytics UI: remove user-facing Zernio vendor branding |
| `eb3da62` | Zernio: public OAuth callback route + multi-profile disconnect |
| `ca2804d` | Docs pass — CONTEXTV18, Maya V09, session logs |
| `93beb2f` | lab5 homepage: Meet Maya hero, cropped product widget cards, FAQ, showpiece spacing |
| `f8da5dd` | Stripe checkout — strip invalid chars from `STRIPE_SECRET_KEY` header |
| `4a6b3f2` | Stripe checkout — recover from stale test-mode customer IDs |
| `3f2793a` | Stripe checkout — accounts without an existing plan |
| `3d6a007` | Auth pages — split layout + mobile marketing panel |
| `31c44d6` | Sign-in / sign-up aligned to lab5 marketing design |
| `81cbe2b` | Google sign-in — reliable profile linking (redirect loop fix) |
| `3f7e1e6` | Fix duplicate `isProductionRuntime` blocking Vercel build |
| `27a03c9` | Launch prep — Meta connect disclosure, legal `.ai` URLs |

## Shipped to `main` (June 15 — consolidated into CONTEXTV18)

| Commit | Summary |
|--------|---------|
| `b9c416b` | Zernio cross-tenant isolation on shared master key |
| `29701e8` | Mobile inbox thread view — header + loading stay visible |
| `be8fad1` | `/data-deletion` page + analytics OAuth / legal updates |
| `4d974da` | Meta domain verification for `agent7even.ai` |
| `5d2dc51` | `PRODUCTION_GREENLIGHT.md` |
| `85099c7` | Remove hero pill badge |

---

## Homepage work (`93beb2f`)

- **Hero:** “Meet *Maya*” (pink) + two-line deck; body copy split; join line + blue primary CTA; Metaballs + dashboard showpiece
- **Cards:** eight `widget-*` mockups in `public/lab5/mockups.js`; clipped inside white cards with left/bottom inset
- **Spacing:** `.showpiece` padding-bottom 80px desktop / 56px default
- **Assets:** `public/agent7even_mark.svg` for mockup sidebar rails

---

## Analytics & Zernio connect (evening)

### Zernio disconnect + OAuth (`eb3da62`)

- **Problem:** Disconnect appeared to succeed in UI but account remained connected; OAuth reconnect looped to sign-in.
- **Fix:** `disconnectPlatformFromTenant()` searches all `zernio_profile_ids`; sync DB via `syncTenantConnectedPlatforms()`; UI verifies API response and refetches accounts.
- **OAuth:** `/api/integrations/zernio/callback` in `proxy.ts` public routes; `oauthCallbackBase()` uses `NEXT_PUBLIC_APP_URL` in production.

### Vendor white-label (`e669826`)

- Removed “Zernio” from follower/post empty states, connect errors, X API modal, OAuth error messages.
- API connect/disconnect error strings neutralized (contact support, not zernio.com).

### Google Analytics OAuth + tab (`0eba105` → `a8ee8c6`)

| Issue | Fix |
|-------|-----|
| “Access pending” with property linked | OAuth path only when `ga_refresh_token` exists; `needsReconnect` vs SA `pending` |
| `redirect_uri_mismatch` on www.agent7even.ai | `gaOAuthRedirectUri(req)` from request host; GCP must whitelist callback URI |
| “Could not get access token” on reconnect | Token exchange error handling; revoke old grant before OAuth; clearer UI errors |
| Hard to separate dev vs real traffic | New **Hostnames** + **Session source / medium** widgets (per tenant property) |

**Files:** `app/api/analytics/ga-data/route.ts`, `ga-connect/route.ts`, `ga-callback/route.ts`, `lib/oauthCallbackBase.ts`, `AnalyticsClient.tsx`, `lib/analytics/mockData.ts`, `lib/maya/summaries/analyticsContext.ts`

**Operator property note:** `538544424` / `G-8913QV8Z1M` — top pages are app routes (`/dashboard/*`, `/sign-in`); referrers include `vercel.com`, `clerk.accounts.dev`, `accounts.google.com` (product dev/testing, not SMB customer traffic).

---

## Docs pass

| File | Action |
|------|--------|
| `CONTEXTV18.md` | Created — supersedes V17; includes vendor gates, Foundation vision, competitive intel |
| `MAYA_CONTEXT_V09.md` | Created — supersedes V08 |
| `SESSION_2026-06-18.md` | This file — consolidated full-day log |
| `AGENTS.md` | Updated pointers + last reviewed date |

Evening update: CONTEXTV18 + this session log extended with analytics/Zernio/GA commits above.

---

## Production ops checklist (GA — manual)

- [ ] GCP `agent7even-analytics` → OAuth client → add `https://www.agent7even.ai/api/analytics/ga-callback`
- [ ] Vercel Production `GOOGLE_OAUTH_CLIENT_SECRET` = full current GCP secret (not masked dashboard copy)
- [ ] Vercel Production `NEXT_PUBLIC_APP_URL` = `https://www.agent7even.ai`

---

## Local dev notes

- Untracked `spikes/` can break `npm run build` (TypeScript picks up `spikes/foundation-creative-ab/run.ts`). Not in repo; CI is clean.
- Dev server: if port 3000 busy, Next may refuse a second instance — use existing process or `kill` stale PID.

---

## Read next

- `CONTEXTV18.md` — full technical state, vendor gates, backlog
- `MAYA_CONTEXT_V09.md` — Maya voice + analytics tab rules
- `PRODUCTION_GREENLIGHT.md` — go-live checklist

---

*Session: June 18, 2026 (consolidated evening pass)*
